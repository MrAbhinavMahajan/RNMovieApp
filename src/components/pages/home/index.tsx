import React from 'react';
import {View} from 'react-native';
import {STYLES} from '../../../constants/Styles';

const HomeScreen = () => {
  return <View style={[STYLES.flex01]}></View>;
};

export default HomeScreen;
